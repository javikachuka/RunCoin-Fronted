import React from 'react';
import { PlayButton } from '../CurrentGame/CurrentGame.elements';

const ButtonClaim = () => {
    return ( 
        <PlayButton  className="claim">
            CLAIM
        </PlayButton>
     );
}
 
export default ButtonClaim;